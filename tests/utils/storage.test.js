import { describe, test } from "vitest";
import { getUsername } from "js/utils/storage.js";

describe(getUsername, () => {
  test("testing that the function returns user name from storage", () => {});
});
